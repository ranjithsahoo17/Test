Structure of the page
Do the three tabs — Configuration, Deviation list, Deviation history — go inside the existing Health PreferencePage, or become a separate page? Turning it into a TabFolder restructures createContents, so I want that agreed before I touch it.
If it's a TabFolder, does the A/C combo stay above the tabs and drive all three, or does each tab get its own selector?
Are the Deviation list and Deviation history tabs part of this same task, or a follow-up? It changes whether I refactor the page now or later.
The checkbox
The tick box is currently rendered inside the Severity cell, not as its own column. Removing it means editing that column's label provider rather than deleting a column — do you want it done that way, or should Severity be rebuilt cleanly?
WCThresholdModel.isEnabled() / setEnabled() — I plan to leave the field and only remove the UI. Is the flag still read anywhere downstream that I should check first?
Is there anything else bound to that checkbox — a select-all, a context menu action, a keyboard handler?
The two new columns
Filtering Law: ComboBoxViewerCellEditor returning a String, to match how LEVEL and TYPE already work with enums. Agreed, or is there a house pattern for combo columns I should follow?
Where do the law names come from when the tree is built — the configuration file, or a server call? If server, that's an EJB call inside the page's load path and I want your view on doing it lazily versus up front.
TE column: plain TextCellEditor, no validation in modify, emptiness checked on Apply. Confirm the check belongs in performOk and not per-cell.
Column widths — the mock-up is much wider than our current build and existing headers are truncated. Is resizing existing columns in scope, or do I only add the two new ones at the declared widths?
Green highlight
Full-row background on the label provider's getBackground, returning null for normal rows. Is there already a Color registry in the plugin I should use rather than allocating and disposing my own?
Existing rows alternate colour via the odd flag on the model. Does the green override that, and does it also override the greyed-out styling on non-editable rows?
Model and state
deviated on WCThresholdModel — who sets it? My assumption is the server tells us at tree load. Is there an existing load path I should extend rather than adding a call?
I've been asked to keep a baseline of the config values. Do you want that on the model, or is there a preference-store snapshot already holding the original configuration I could read instead?
Process
What's the branch and commit convention for a BCR — one branch, or one per commit set?
Are there existing UI tests on this page, or is validation manual? Affects whether I can safely restructure createContents.
Who reviews changes in com.eurocopter.mgs.presentation.configuration.preferences.healthalarms? I'd like their view on the tab restructure before I write it.
